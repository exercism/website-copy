# decodeComment uses helper functions

Please use `decodeId`, `decodePullRequestReviewId`, `decodeUser`, `decodeSide` and `decodeLinks` in `decodeComment`, as reusing functions helps with maintainability.
