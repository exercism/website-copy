# use getter-only property

The `%{name}` property can be defined as a _getter-only_ property, where the setter is omitted.
You'll still be able to assign a value to the property, but only from the constructor.
