# `increase_quantity` best function choice

Use either the `Map.new/2` function or the `Enum.into/3` function in the `increase_quantity` function. They're the best fitting functions for this task and they don't require the usage of the `Enum.map/2` function.
