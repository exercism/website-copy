# `apply_discount` function reuse

Use the function `apply_discount/2` to apply the discount in functions `monthly_rate/2` and `days_in_budget/3`. Code reuse is crucial to writing maintainable codebases.
